# Day 09 — Builder and Artifact Validation

**Status:** `COMPLETE`

The Builder produces a versioned source artifact through the ArtifactStore. The artifact receives a content hash and is evaluated from a separate temporary execution location.

**Tests:** artifact creation and full experiment passed.
