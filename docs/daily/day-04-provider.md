# Day 04 — Offline Provider and Configuration

**Status:** `COMPLETE`

The provider protocol and deterministic `MockProvider` are implemented. The first run requires no credentials and produces stable metadata for tests. External providers remain behind the same interface.

**Tests:** included in the full suite; offline CLI run passed.
